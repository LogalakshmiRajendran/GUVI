#include<stdio.h>
void main()
{
int arr[N];
int i;
intsum=0;
printf("program to find an array of N numbers:");
for(i=0;i<N;i++)
{
printf("enter number.%d:\n",i+1);
scanf("%d",&arr[i]);
}
for(i=0;i<N;i++)
sum=sum+arr[i];
}
printf("sum:5d",sum);
}
