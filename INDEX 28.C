#include<stdio.h>
int main()
{
int i,int arr[50],num;
printf("\n enter number of elements:");
scanf("%d",&num);
printf("\n enter the values:");
for(i=0;i<num;i++)
}
scanf("%d",&ar[i]);

for(i=0;i<num;i++)
{
printf("\narr[%d]=%d",i,arr[i]);
}
return(0);
}
